
interface VisiableFilterI {
    all: string;
    complete: string;
    inComplete: string
}

export const visiableFilter: VisiableFilterI = {
    all : 'all',
    complete: 'complete',
    inComplete: 'inComplete'
};


